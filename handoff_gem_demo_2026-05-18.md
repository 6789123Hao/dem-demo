# GEM DEMO 交接记录 - 2026-05-18

## 当前仓库状态

- 本地目录：`C:\Users\ROG\Desktop\GEM DEMO`
- GitHub 仓库：`https://github.com/6789123Hao/dem-demo.git`
- 当前主分支：`main`
- 已完成首次提交和推送，远程仓库已经包含本地 GEM DEMO 文件夹内容。
- 远程仓库原本已有初始化提交，本地推送前通过同步/处理后完成上传。

## 主要原型文件

- `htmlDemo简单互动玩法/宝石鉴定html_v21玩法出入镜头版本_1.html`
  - 旧版综合玩法原型。
  - 保留作对照，不作为当前主循环方向继续扩展。

- `htmlDemo简单互动玩法/宝石鉴定html_v22核心玩法主循环版.html`
  - 当前重点版本。
  - 已改为主 UI 内完成核心玩法，不再跳转到独立玩法页。

## v22 当前玩法方向

- 只保留一种核心玩法方向：
  - 平面宝石展示。
  - 手动切 Lens。
  - 自动观察面。
  - 手选工具。
- 每颗素体按 `GEM_BLUEPRINTS.tasks` 全量执行；当前正式流程允许 1-4 次交互，以兼容孔雀石、绿松石这类“清洁 → 切除 → 定形 → 收光”的原石链路。

- 旧的 FP、2D/3D 多玩法选择、独立玩法页面逻辑不再作为主流程入口。
- 核心玩法直接发生在主 UI 中间的镜片区域。

## 主 UI 结构

- 左侧：素体袋。
  - 可拖拽到中间镜片区，手动玩一颗优先初见素体。
  - 可拖拽到下方批量入口管道，批量处理整袋素体。
  - 带“打开背包”按钮。

- 左侧背包浮层：
  - 通过按钮打开。
  - 内部有关闭按钮。
  - 已知种类素体可以拖到镜片区，直接查找并手动处理该类型。

- 中间：镜片位置 / 核心玩法区域。
  - 空闲时显示投放提示。
  - 进入玩法后在同一区域显示宝石、瑕疵点、镜头和工具 UI。
  - 不再切换到独立 `screen-play` 页面。

- 下方：批量入口管道。
  - 素体袋拖入后进入批量处理流程。

- 右下：盘子与奖励。
  - 盘子区域已放大。
  - 完成出石后，小型宝石会一颗一颗以类似骰子掉落/弹跳的动画加入盘子。
  - “收集奖励”按钮已放到盘子上方，并做成更明显的大按钮。

## 核心玩法交互

- 鼠标滚轮：切换 Lens。
- 鼠标右键：循环切换工具。
- 左键点击：点击当前可见瑕疵点并尝试完成当前步骤。
- 只有在正确 Lens 下，当前瑕疵点和引导圈才会显示。
- 工具错误或 Lens 错误时不会完成步骤。
- 最后一步完成后会立即清除瑕疵点，避免结算前残留。

## 奖励流程

- 单颗或一轮完成后，不再自动弹奖励页。
- 成石先累计到右下盘子。
- 点击盘子上方“收集奖励”后，才弹出主 UI 内奖励弹窗。
- 弹窗显示当前累计的全部奖励。
- 确认收集后清空待收集奖励和盘子，并停留在主 UI。

## GitHub 推送过程记录

- 初次 `git commit` 前遇到作者身份未配置，已设置：
  - `user.email = 785009889@qq.com`
  - `user.name = 6789123Hao`

- 初次 SSH 推送遇到：
  - `Permission denied (publickey)`
  - 原因是本机 SSH key 未配置到 GitHub。

- 改用 HTTPS 后遇到 443 连接问题。
  - 后续确认代理工具的 `Mixed Port` 是 `12334`。
  - 使用代理环境变量连接 GitHub：
    - `HTTP_PROXY=http://127.0.0.1:12334`
    - `HTTPS_PROXY=http://127.0.0.1:12334`
    - `GIT_SSL_BACKEND=openssl`

- HTTPS 推送需要 GitHub 凭据。
  - GitHub 不接受账号密码，需要 Personal Access Token。
  - Token 已由用户在本地 Git Bash 输入，未记录在本文件中。

- 推送时远程已有提交，出现过 non-fast-forward。
  - 处理远程差异后，最终成功推送到 GitHub。

## 后续建议

- 2026-05-18 本轮更新：
  - 已核对两个 handoff，并以 `htmlDemo简单互动玩法/codex_handoff_宝石鉴定原型.md` 中“最终版 12 种宝石 / 推荐流程配置”为宝石数据准绳。
  - `宝石鉴定html_v22核心玩法主循环版.html` 已补齐孔雀石、绿松石的最后 `finish` 收光步骤。
  - `buildCoreTasks()` 已从只取前三步改为执行配置内全部步骤，避免四步正式流程被静默截断。
  - 当前目录是从压缩包展开的工作副本，没有 `.git` 目录；如果要继续推送 GitHub，需要在真正的仓库目录或重新 clone 的仓库里提交。
- 2026-05-18 交互反馈追加：
  - 修补水滴点击 3 次，每次点击后水滴缩小一档，提示玩家已点过。
  - 收光处理点现在在任意 Lens 都可见并可点击，不再需要为了收光切镜头。
  - 清洁 / 切除 / 定型拖拽时会从鼠标位置掉落短寿命小特效，增强“正在拖拽”的反馈。
- 下一轮如果继续迭代，优先直接修改 `宝石鉴定html_v22核心玩法主循环版.html`。
- 如需进一步稳定版本，可以另存 `v23`，避免破坏当前 v22。
- 若继续推送 GitHub，建议在 Git Bash 中沿用代理：

```bash
export HTTP_PROXY=http://127.0.0.1:12334
export HTTPS_PROXY=http://127.0.0.1:12334
export GIT_SSL_BACKEND=openssl
git -c http.sslBackend=openssl push
```

## 2026-05-19 x22 鉴定流水线加强版

- 新增当前主迭代文件：
  - `htmlDemo鉴定加强版/宝石鉴定html_x22鉴定流水线加强版.html`
  - 命名使用 `x22`，避免和稳定主循环 `v22` 混淆。
- 玩法方向已经从“逐颗修复宝石”转为：
  - 先识别未知素体。
  - 检验完成后在左下角档案记录名称、特性与推荐流程。
  - 再通过三段可翻转管道运行已知批量。
  - 批量只处理“已识别 + 管道流程匹配”的类型。
- 主入口结构：
  - 打开 UI 后，中间桌面直接出现袋内存在的素体，按自然撒落位置分布，并带入场撒落动画。
  - 桌面左下角有大号提示“点击石头↗”。
  - 点击桌面石头后进入观测态：散落石头隐藏，选中石头进入放大镜环境。
  - 观测态可点击选中石头或点击“检验”按钮进入检验玩法。
  - 观测态右上角缩小按钮、向下滚轮都可返回桌面视角。
- 左侧 UI：
  - 改为“鉴定档案”列表。
  - 未检验类型显示未知档案占位。
  - 检验完成后立刻退出玩法回桌面，左下档案更新并高亮。
- 检验玩法目前包含四步：
  - 放大镜：宝石放大；点击两处不同位置，左下角生成显微纹理证据卡。
  - 透光：拖动手电筒，手电永远朝向宝石；只有正确角度才在对面出现透光点，松手完成。
  - 硬度：宝石放大；划片需要从左到右以较慢、稳定的速度划过，失败后划片回到左侧起点。
  - 称重：简化为 3 个样品/目标石排序；目标石轻中重按宝石类型变化，不再固定为中间数。
- 证据演出：
  - 每一步完成都会在左下角追加一张证据卡。
  - 证据卡不再向上排队，而是像纸张一样覆盖叠放，带轻微左右旋转并露出后面的纸边。
  - 最终检验完成时，全部证据一起归档并更新左侧档案。
- Lens 可读性：
  - 当前步骤 Lens 不对时，对应道具变暗禁用。
  - 同时显示通用“鼠标下滚轮切 Lens”提示。
- 批量管道：
  - 底部三段翻转管道仍为 `A/B -> C/D -> E/F`。
  - 按钮文案为“运行已知批量”。
  - 只会完成已识别且流程匹配的宝石，不匹配或未知会退回/留在桌面。
